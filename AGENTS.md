# 项目协作与 Agent 行为约束

## 🇨🇳 语言与沟通规范
- 日常交流、代码注释和提交说明使用简体中文。
- 与用户沟通时使用容易理解的简体中文。提供技术实现选项时，先用日常语言说明每个选项的实际效果、优缺点和适用场景，再补充必要的专业术语，并明确给出推荐及理由；不得只罗列技术名词让用户选择。

## 🚫 授权与文件红线
- 只有用户明确要求“修改”“修复”“实施”或“写代码”时才修改仓库文件；未获授权时只读分析。
- 未经用户明确要求不得修改本文件；本文件只保留跨任务、长期稳定的协作红线与权威指针，不得写入或复制具体实现参数、命令清单或阶段性事实。
- 交互原型真源唯一为 `docs/prototypes/prototype.html`；新页面必须在该文件的左侧导航栏追加 Tab 演进，严禁新建散落的 HTML 副本；历史状态交给 Git，不做手工归档复制。
- 严禁引入成套重型测试体系（UI 测试、E2E、CI 门禁等）；仅允许为关键帧规划算法编写唯一一个纯函数单测（`RetentionPlanner.test.ts`，< 50 行），用于校验向左吸附数学不变量。

## 🔧 Git 行为规范
- 不自动执行 Git commit、push 或创建 PR；只有用户主动要求时才做。
- PowerShell 下含中文的提交说明先写 UTF-8 无 BOM 文件，再用 `git commit -F`，不要直接在命令行 `-m` 传入或通过管道传入，杜绝乱码。
- 不得提交任何密钥、临时日志、媒体切片缓存或本地用户数据。
- 同一时间只运行一个 Git 相关 shell 任务，不得并行反复扫描，只结束已确认属于本会话的遗留进程。

## 📖 事实与真源边界
- 当前文档遵守“一项事实一个真源”；文档导航与生命周期按 [`README.md`](README.md#文档导航) 执行，其他文档只链接、不复制。
- 业务名词与概念统一查阅 [`CONTEXT.md`](CONTEXT.md)，严禁自造别名或在代码中混淆术语；ADR 只记录长期不可逆的重大决定。
