# 方案：视频切片首帧封面自适应与偏好持久化

- **方案编号**：`20260904-视频切片首帧封面自适应与偏好持久化`
- **创建日期**：2026-09-04
- **完成日期**：2026-09-04
- **当前状态**：已实施并验收通过 (Archived)
- **归档路径**：`archive/docs/方案/20260904-视频切片首帧封面自适应与偏好持久化.md`

---

## 1. 背景与问题诊断

在多段视频裁剪导出后，用户发现所有切出的子视频在 Windows 资源管理器中的封面缩略图完全一致，无法根据封面区分各个片段。

### 根本原因
- 许多网络视频（如 Bilibili 下载的 MP4）在主动态视频流外，内嵌了一条静态附加图片流（Stream #0:2: Video: mjpeg, `[attached_pic]`），作为全局视频封面；
- 系统在调用 FFmpeg 进行流拷贝（`-c copy`）时，默认采用了 `-map 0`，导致该静态封面流被无差别复制进每一个导出的切片视频中；
- Windows 媒体缩略图提取器优先抓取 `attached_pic` 作为文件图标，导致即便每个分段的实际画面截然不同，系统呈现的缩略图却千篇一律。

---

## 2. 解决方案与设计

### 2.1 核心策略
1. **流剥离机制**：在执行剪辑命令时，将参数调整为 `-map 0:V -map 0:a?`。精准复制所有动态视频流（大写的 `V` 专门排除静态图片流）和可选音频流，自动剥离内嵌的 `attached_pic`；
2. **偏好设置可控**：支持用户自主选择是否开启该策略，提供设置项并持久化保存至方案文件；
3. **首帧画面自然生效**：剥离固定封面后，操作系统在生成缩略图时会自动回退到视频第一帧有效画面，各个切片视频自然呈现出各具特色的首帧内容。

---

## 3. 改动文件与实现细节

| 组件 / 层级 | 文件路径 | 变更说明 |
| :--- | :--- | :--- |
| **共享领域模型** | [`src/shared/types.ts`](../../src/shared/types.ts) | 在 `AppConfig`、`MediaRetentionPlan` 和 `PlanRecord` 中新增 `stripOriginalCover: boolean`（默认为 `true`）以及 `concatSingleFile?: boolean`。 |
| **关键帧规划器** | [`src/shared/RetentionPlanner.ts`](../../src/shared/RetentionPlanner.ts) | `planRetention` 支持传入 `stripOriginalCover` 并写入返回的剪辑计划对象中。 |
| **剪辑执行器** | [`src/main/services/FFmpegExecutor.ts`](../../src/main/services/FFmpegExecutor.ts) | 当 `stripOriginalCover !== false` 时，执行命令采用 `-map 0:V -map 0:a?`；当为 `false` 时回退至 `-map 0` 保留原封面。 |
| **方案存储与执行** | [`src/main/services/PlanStore.ts`](../../src/main/services/PlanStore.ts) | 方案中心单任务或批量执行时，完整读取方案中保存的 `stripOriginalCover` 设置。 |
| **前端工作台** | [`src/renderer/components/CutterPage.tsx`](../../src/renderer/components/CutterPage.tsx) | 底部操作 Dock 栏新增 **`[✓] 自适应首帧封面`** 勾选框；方案保存、编辑回载、执行时双向同步该偏好。 |

---

## 4. 验收测试结论与成果实证

在真实测试样本 `苒苒不想打咩了B站\items\BV1GTvWBzEg9\` 执行裁剪验证：

### 4.1 界面偏好设置验证
底部 Dock 栏已具备 `[✓] 自适应首帧封面` 偏好勾选框，保存与回载正常：
![封面偏好UI配置](d:/Projects/litleTools/videoCutTool_tmp/screenshots/cover_preference_ui.png)

### 4.2 物理流审计（FFprobe）
- 导出的 `_seg01.mp4` 和 `_seg02.mp4` 经 FFprobe 探查，已完全剥离 `attached_pic` 静态图片流，仅保留原生动态视频流 `h264` 与音频流 `aac`。

### 4.3 真实封面缩略图对比
- **分段 1 (`_seg01.mp4`) 首帧封面**（第 0 秒开场画面）：
  ![分段1首帧](d:/Projects/litleTools/videoCutTool_tmp/screenshots/seg01_cover.jpg)

- **分段 2 (`_seg02.mp4`) 首帧封面**（第 85 秒古风特写画面）：
  ![分段2首帧](d:/Projects/litleTools/videoCutTool_tmp/screenshots/seg02_cover.jpg)

**结论**：在 Windows 资源管理器中，各个切片视频呈现出截然不同、对应各自实际内容的第一帧画面，彻底告别了封面千篇一律！
